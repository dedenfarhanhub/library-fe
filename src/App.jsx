import './App.css'

export default function App() {
    return (
        <div className="min-h-screen flex items-center justify-center bg-gray-100">
            <h1 className="text-3xl font-bold text-blue-500">
                React + Vite + Tailwind v4 + ESLint + Prettier 🚀
            </h1>
        </div>
    );
}
